import {Header} from './Header.tsx';
import {Body} from './Body.tsx';
import {Footer} from './Footer.tsx';

export const Site = () => {
    return (
        <>
            <Header title={'Header'}/>
            <Body title={'Body'}/>
            <Footer title={'Footer'}/>
        </>
    )
}